# Daily notes are always edited locally — keep local version on merge conflicts
"02 Daily/**/*.md" merge=ours

# Update files are owned by the cloud routine — always take remote on merge conflicts
"01 Updates/*.md" merge=theirs
